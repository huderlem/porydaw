# Keyboard Shortcuts

## The shortcut editor

<!-- TODO: Edit → Settings → Keyboard Shortcuts (Ctrl+, / ⌘,); search,
rebinding, conflicts
(steal-on-assign), restore defaults; mouse-gesture commands (e.g. the
velocity drag chord) are rebindable too. -->

## Default shortcuts

<!-- TODO: Tables of defaults by category, matching the dialog's grouping
(File, Transport, Editing, View, Navigation, ...). Consider generating this
table from the keymap registry so it can't drift — a small tools/ script
could dump it as Markdown. Note macOS Cmd vs. Ctrl convention once at the
top instead of per-row. -->
