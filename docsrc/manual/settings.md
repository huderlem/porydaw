# Settings

<!-- TODO: Edit → Settings (Ctrl+, / ⌘,): every app-wide preference in one
window — sections on the left, the selected section on the right. Nothing
is staged: each control applies the moment you change it, and Close just
puts the window away. The window reopens on the section you used last. -->

## Audio

<!-- TODO: Output level (porydaw's own listening level, 0–200%,
double-click resets to 100%; never saved with the song, never in exported
WAVs — see [Playing Songs](playback.md)). GBA engine: PCM polyphony, PCM
mix rate, analog output filter — the global accuracy knobs, contrast with
the per-song [Song Settings](song-settings.md). Restore Defaults. -->

## Appearance

<!-- TODO: Theme presets + Custom colors, grid line contrast, system font.
Keep the canonical explanation in [Themes & Appearance](appearance.md) and
cross-link. -->

## Keyboard Shortcuts

<!-- TODO: Pointer to [Keyboard Shortcuts](shortcuts.md). -->
