# Playing Songs

## Play, pause, and stop

<!-- TODO: Transport buttons + Space; difference between pause and stop;
where playback starts (edit cursor). -->

## The playhead and the edit cursor

<!-- TODO: Two cursors, why: playhead = where audio is, edit cursor = where
you're working / where paste lands / where playback starts. Clicking the
timeline vs. clicking the roll. -->

## Looping

<!-- TODO: The loop toggle; the song's own loop region ([ / ] markers) and
how GBA songs loop forever in-game; seamless loop audition. -->

## Follow playhead

<!-- TODO: The transport toggle (also View menu): on = the view scrolls with
playback; off = the camera stays put. Persisted. -->

## Output level

<!-- TODO: Edit → Settings → Audio → Output level (0–200%, double-click
resets to 100%) — listening volume only, never saved into the song, never
in exported WAVs. Contrast with track volume and the song's master volume
setting in [Song Settings](song-settings.md). -->

## It sounds exactly like the game

<!-- TODO: Short explainer that playback runs the real m4a engine with the
project's own settings — reverb, polyphony limit, sample rate. If something
sounds "wrong", it will sound wrong in-game too, and that's the feature.
Link to [Polyphony](polyphony.md). -->
